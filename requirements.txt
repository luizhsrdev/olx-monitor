requests>=2.31
PyYAML>=6.0
python-dotenv>=1.0

# Opcional: só necessário se algum monitor usar `modo: playwright`.
# Depois de instalar, rode também: playwright install chromium
playwright>=1.44

pytest>=8.0

# Opcional: só necessário para o painel local de edição (app.py).
# Rode com: streamlit run app.py
streamlit>=1.35
